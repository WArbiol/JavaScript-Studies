import moment from "moment/src/moment"

alert(moment().format("HH:mm:ss"))

/* 

   Webpack - static module bundler
   Lê o código todo e cria outro código
   só que mais leve: parece que se importamos
   uma biblioteca ele cria um arquivo apenas
   com as funções necessárias da biblioteca 

*/
